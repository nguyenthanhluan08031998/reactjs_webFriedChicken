const db = require('../utils/db');

module.exports = {
    // getAllCartById: (IdUser) => db.load(`select * from Cart, Product where Cart.IdUser = Product.Id = ${IdUser} and Card.IsOrdered = 0`),
    // deleteProductInCart: (table, id) => db.delete(table, id)
}