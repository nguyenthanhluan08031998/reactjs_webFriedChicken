import { createMuiTheme } from "@material-ui/core/styles";
import defaultTheme from "./default";

//Custom global theme for Material go here
const theme = createMuiTheme(defaultTheme);
export default theme;
